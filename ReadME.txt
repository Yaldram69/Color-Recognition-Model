#open pip and run this command
pip install -r requirements.txt

#If you want to generate your own dataset, run the Color Generator.py file.

#If you want to train the model with your own parameter, after the libraries have
been installed, run the Train.py file.

#After the model is trained, run the Test.py file.

#Make the dataset diverse with multiple shades for better classification.
#Add color ranges of your choice if needed.
#Increase the parameters if you have better Computational Power or a GPU.